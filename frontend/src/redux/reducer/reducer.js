let initialState = {
    id: '',
    pwd: '',
    nickname: '',
    isPlaying: true
};

function reducer(state = initialState, action) {
    switch (
        action.type
        // case 'INPUT':
        // return { ...state, id: 'junik' };
    ) {
    }
}

export default reducer;
