import Button from 'react-bootstrap/Button';
import { Bell } from 'react-bootstrap-icons';

const TopbarAlarm = () => {
    return (
        <Button variant="light">
            <Bell />
        </Button>
    );
};

export default TopbarAlarm;
